import React from 'react';
import Modal from '../modal/Modal';
// import styles from './App.css';

export type SomeType = {
    foo: string;
    bar: number;
};

const App = () => (
    <div>
        <Modal />
    </div>
);

export default App;
